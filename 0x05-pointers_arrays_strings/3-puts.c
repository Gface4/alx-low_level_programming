#include "main.h"

/**
 * _puts - print a string to the stdout
 * @str: the string
 * Return the lenght of the string
 */

void _puts(char *str)
{
	int a = 0;

	while (stra[a] != '\10')
	{
		_putchar(str[a];
		a++
	}
	_putchar('\n');
}
